//! Поддерживаемые языки интерфейса.
//!
//! Вынесен в отдельный модуль, чтобы `build.zig` мог использовать этот enum
//! для build-опции `-Dlocale` без зависимости от модуля `build_options`.

pub const Lang = enum {
    ru,
    en,
    es,
    fr,
};
